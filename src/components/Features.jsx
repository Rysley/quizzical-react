import React from "react";

export default function Features() {
  return (
    <section className="features">
      <ul className="features__list">
        <li className="features__list-item">package.json configured</li>
        <li className="features__list-item">Sass compiler installed</li>
        <li className="features__list-item">Selected node_modules from Vite</li>
        <li className="features__list-item">Vite bundler - Rollup</li>
        <li className="features__list-item">No git attachement</li>
      </ul>
    </section>
  );
}
